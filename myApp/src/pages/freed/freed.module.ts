import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { FreedPage } from './freed';

@NgModule({
  declarations: [
    FreedPage,
  ],
  imports: [
    IonicPageModule.forChild(FreedPage),
  ],
})
export class FreedPageModule {}
